# SPDX-FileCopyrightText: 2025 SAP edge team
# SPDX-FileContributor: Manjun Jiao (@mjiao)
#
# SPDX-License-Identifier: Apache-2.0

---
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: rosa-validate-and-create-configmap
spec:
  description: >-
    Creates a ConfigMap with cluster info for endpoint tests (host, ingressIP from
    istio-ingressgateway LB). Must run AFTER EIC is deployed and MetalLB assigns the IP.
  params:
    - name: clusterName
      type: string
      description: "ROSA cluster name"
    - name: awsRegion
      type: string
      description: "AWS region"
      default: "eu-north-1"
    - name: awsSecretName
      type: string
      description: "Name of the Kubernetes Secret containing AWS credentials"
      default: "aws-credentials-secret"
    - name: redhatTokenSecretName
      type: string
      description: "Name of the Kubernetes Secret containing Red Hat OCM token"
      default: "redhat-token-secret"
  workspaces:
    - name: source
  results:
    - name: cluster-summary
      description: "Brief cluster and services validation summary"
  steps:
    - name: validate-and-create-configmap
      image: registry.access.redhat.com/ubi9/ubi
      workingDir: $(workspaces.source.path)
      timeout: "336h"
      env:
        - name: CLUSTER_NAME
          value: "$(params.clusterName)"
        - name: AWS_REGION
          value: "$(params.awsRegion)"
        - name: AWS_DEFAULT_REGION
          value: "$(params.awsRegion)"
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: $(params.awsSecretName)
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: $(params.awsSecretName)
              key: AWS_SECRET_ACCESS_KEY
        - name: REDHAT_OCM_TOKEN
          valueFrom:
            secretKeyRef:
              name: $(params.redhatTokenSecretName)
              key: REDHAT_OCM_TOKEN
      script: |
        #!/usr/bin/env bash
        set -euo pipefail

        echo "✅ Creating endpoint test ConfigMap from istio-ingressgateway..."
        echo "=============================================="

        # Install required packages
        echo "📦 Installing required packages..."
        dnf install -y jq wget tar > /dev/null 2>&1

        # Install ROSA CLI
        echo "📦 Installing ROSA CLI..."
        wget -q https://mirror.openshift.com/pub/openshift-v4/clients/rosa/latest/rosa-linux.tar.gz
        mkdir -p /tmp/rosa-install
        tar xzf rosa-linux.tar.gz -C /tmp/rosa-install
        mv /tmp/rosa-install/rosa /usr/local/bin/rosa
        rm -rf /tmp/rosa-install rosa-linux.tar.gz

        # Install OpenShift CLI
        echo "📦 Installing OpenShift CLI..."
        curl -sL https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux.tar.gz -o /tmp/oc.tar.gz
        tar xzf /tmp/oc.tar.gz -C /tmp
        mv /tmp/oc /usr/local/bin/
        cp /usr/local/bin/oc /usr/local/bin/kubectl
        rm -f /tmp/oc.tar.gz
        oc version --client
        rosa version

        # Login to Red Hat OCM
        echo "🔐 Logging into Red Hat OpenShift Cluster Manager..."
        rosa login --token="${REDHAT_OCM_TOKEN}"

        # Get cluster info
        echo "📋 Getting ROSA cluster information..."
        rosa describe cluster -c "${CLUSTER_NAME}" -o json > cluster-info.json
        API_URL=$(jq -r '.api.url' cluster-info.json)
        echo "API URL: ${API_URL}"

        # Use a separate kubeconfig for the ROSA cluster so the default
        # in-cluster config (management cluster) remains available via plain oc.
        export KUBECONFIG="/tmp/rosa-kubeconfig"

        # Try to use existing kubeconfig from source workspace
        echo "🔐 Looking for kubeconfig in workspace..."
        if [[ -f "kubeconfig" ]]; then
          echo "✅ Found kubeconfig in workspace"
          cp "$(pwd)/kubeconfig" "${KUBECONFIG}"
          oc whoami || echo "Kubeconfig may need refresh"
        else
          echo "⚠️ No kubeconfig found, trying to login with ROSA credentials..."

          # Try to get admin credentials
          if rosa describe admin -c "${CLUSTER_NAME}" &>/dev/null; then
            # Try to recreate admin to get fresh credentials
            rosa delete admin -c "${CLUSTER_NAME}" --yes 2>/dev/null || true
            sleep 5
          fi

          rosa create admin -c "${CLUSTER_NAME}" -o json > admin-creds.json 2>&1 || true

          if [[ -f "admin-creds.json" ]] && jq -e . admin-creds.json >/dev/null 2>&1; then
            ADMIN_USERNAME=$(jq -r '.username // "cluster-admin"' admin-creds.json)
            ADMIN_PASSWORD=$(jq -r '.password // ""' admin-creds.json)

            if [[ -n "${ADMIN_PASSWORD}" ]] && [[ "${ADMIN_PASSWORD}" != "null" ]]; then
              echo "🔐 Attempting login with admin credentials..."
              for attempt in 1 2 3 4 5; do
                if oc login "${API_URL}" -u "${ADMIN_USERNAME}" -p "${ADMIN_PASSWORD}" --insecure-skip-tls-verify=true; then
                  echo "✅ Login successful!"
                  break
                fi
                WAIT_TIME=$((attempt * 15))
                echo "⏳ Waiting ${WAIT_TIME}s before retry..."
                sleep $WAIT_TIME
              done
            fi
          fi
        fi

        echo "🔍 Verifying access to ROSA cluster..."
        oc get nodes || echo "⚠️ Could not list nodes"
        echo "✅ Connected to ROSA cluster"

        # Get ingress domain from OpenShift ingress controller
        echo "📋 Getting ingress domain..."
        INGRESS_DOMAIN=""

        # Try to get ingress domain from ingress controller
        if oc get ingresscontroller default -n openshift-ingress-operator -o json &>/dev/null; then
          INGRESS_DOMAIN=$(oc get ingresscontroller default -n openshift-ingress-operator \
            -o jsonpath='{.status.domain}' 2>/dev/null || echo "")
          echo "Ingress Domain: ${INGRESS_DOMAIN}"
        fi

        # Fallback: Get from DNS config
        if [[ -z "${INGRESS_DOMAIN}" ]]; then
          INGRESS_DOMAIN=$(oc get dns cluster -o jsonpath='{.spec.baseDomain}' 2>/dev/null || echo "")
          if [[ -n "${INGRESS_DOMAIN}" ]]; then
            INGRESS_DOMAIN="apps.${INGRESS_DOMAIN}"
          fi
          echo "Ingress Domain (from DNS): ${INGRESS_DOMAIN}"
        fi

        # Detect istio-ingressgateway namespace
        echo ""
        echo "📋 Locating istio-ingressgateway service..."
        ISTIO_NS=""
        if oc get svc istio-ingressgateway -n istio-gateways &>/dev/null; then
          ISTIO_NS="istio-gateways"
        elif oc get svc istio-ingressgateway -n istio-system &>/dev/null; then
          ISTIO_NS="istio-system"
        else
          # Fallback: search all namespaces
          ISTIO_NS=$(oc get svc --all-namespaces -o json | jq -r '.items[] | select(.metadata.name=="istio-ingressgateway") | .metadata.namespace' | head -1)
        fi

        if [[ -z "${ISTIO_NS}" ]]; then
          echo "⚠️ WARNING: Could not find istio-ingressgateway in any namespace"
          oc get svc --all-namespaces | grep -i istio || echo "No istio services found"
        else
          echo "✅ Found istio-ingressgateway in ${ISTIO_NS} namespace"
        fi

        # Annotate istio-ingressgateway for AWS NLB (required for ROSA)
        if [[ -n "${ISTIO_NS}" ]]; then
          echo ""
          echo "📋 Annotating istio-ingressgateway for AWS NLB..."
          oc annotate svc istio-ingressgateway -n "${ISTIO_NS}" \
            service.beta.kubernetes.io/aws-load-balancer-type="nlb" \
            --overwrite
          echo "✅ NLB annotation applied"

          # Wait for AWS to provision the NLB
          echo "⏳ Waiting for NLB to be provisioned..."
          NLB_TIMEOUT=300
          NLB_INTERVAL=10
          NLB_ELAPSED=0
          while [[ ${NLB_ELAPSED} -lt ${NLB_TIMEOUT} ]]; do
            NLB_ADDR=$(oc get svc istio-ingressgateway -n "${ISTIO_NS}" \
              -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || echo "")
            if [[ -z "${NLB_ADDR}" ]]; then
              NLB_ADDR=$(oc get svc istio-ingressgateway -n "${ISTIO_NS}" \
                -o jsonpath='{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
            fi
            if [[ -n "${NLB_ADDR}" ]]; then
              echo "✅ NLB provisioned: ${NLB_ADDR}"
              break
            fi
            echo "  Waiting... (${NLB_ELAPSED}s/${NLB_TIMEOUT}s)"
            sleep ${NLB_INTERVAL}
            NLB_ELAPSED=$((NLB_ELAPSED + NLB_INTERVAL))
          done
          if [[ ${NLB_ELAPSED} -ge ${NLB_TIMEOUT} ]]; then
            echo "⚠️ WARNING: NLB provisioning timed out after ${NLB_TIMEOUT}s"
          fi
        fi

        # Get LoadBalancer address from istio-ingressgateway service
        echo ""
        echo "📋 Getting LoadBalancer address from istio-ingressgateway..."
        INGRESS_IP=""

        if [[ -n "${ISTIO_NS}" ]]; then
          # Try IP first, then hostname (for cloud providers like AWS)
          INGRESS_IP=$(oc get svc istio-ingressgateway -n "${ISTIO_NS}" \
            -o jsonpath='{.status.loadBalancer.ingress[0].ip}' 2>/dev/null || echo "")
          if [[ -z "${INGRESS_IP}" ]]; then
            INGRESS_IP=$(oc get svc istio-ingressgateway -n "${ISTIO_NS}" \
              -o jsonpath='{.status.loadBalancer.ingress[0].hostname}' 2>/dev/null || echo "")
          fi
        fi

        if [[ -z "${INGRESS_IP}" ]]; then
          echo "⚠️ WARNING: Could not get LoadBalancer address from istio-ingressgateway"
          echo "   Make sure EIC is deployed and the LoadBalancer is provisioned"
          oc get svc --all-namespaces | grep -i istio || echo "No istio services found"
        else
          echo "✅ LoadBalancer address: ${INGRESS_IP}"
        fi

        echo ""
        echo "📊 Ingress Configuration:"
        echo "  Domain: ${INGRESS_DOMAIN:-'Not determined'}"
        echo "  LoadBalancer IP: ${INGRESS_IP:-'Not determined'}"

        # Build the EIC host URL
        EIC_HOST=""
        if [[ -n "${INGRESS_DOMAIN}" ]]; then
          # Standard EIC pattern: eic.{ingress-domain}
          EIC_HOST="eic.${INGRESS_DOMAIN}"
        fi

        echo "  EIC Host: ${EIC_HOST:-'Not configured'}"

        # Create ConfigMap for endpoint tests on the MANAGEMENT cluster
        # (where Tekton runs and where test task pods will be scheduled).
        # Unset KUBECONFIG so oc falls back to the in-cluster config.
        CONFIGMAP_NAME="cluster-info-rosa-${CLUSTER_NAME}"
        echo ""
        echo "📝 Creating ConfigMap '${CONFIGMAP_NAME}' on the management cluster for endpoint tests..."

        # Delete existing ConfigMap if present
        KUBECONFIG="" oc delete configmap "${CONFIGMAP_NAME}" --ignore-not-found=true

        # Create the ConfigMap (matching expected format: host + ingressIP only)
        KUBECONFIG="" oc create configmap "${CONFIGMAP_NAME}" \
          --from-literal=host="${EIC_HOST:-eic.apps.${CLUSTER_NAME}.local}" \
          --from-literal=ingressIP="${INGRESS_IP:-127.0.0.1}"

        echo "✅ ConfigMap created successfully on management cluster"
        KUBECONFIG="" oc get configmap "${CONFIGMAP_NAME}" -o yaml

        # Generate summary
        SUMMARY="ROSA Cluster: ${CLUSTER_NAME} | Domain: ${INGRESS_DOMAIN:-N/A} | Ingress: ${INGRESS_IP:-N/A}"
        echo -n "${SUMMARY}" > $(results.cluster-summary.path)

        echo ""
        echo "✅ ROSA ConfigMap creation completed!"
        echo "📋 Summary:"
        echo "  Cluster: ${CLUSTER_NAME}"
        echo "  ConfigMap: ${CONFIGMAP_NAME}"
        echo "  EIC Host: ${EIC_HOST:-Not configured}"
        echo "  LoadBalancer IP (istio-ingressgateway): ${INGRESS_IP:-Not determined}"
